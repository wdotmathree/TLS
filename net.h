#include "defines.h"

extern uint16_t curr_suite;
extern char *curr_key;

// int plaintext_send(int socket, const char *data, const size_t len, const uint8_t type);

// int plaintext_recv(int socket, char *data);

// int handshake_send(int socket, const char *data, const size_t len, const uint8_t type);

// int handshake_recv(int socket, char *data);
